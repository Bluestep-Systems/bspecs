# Tasks — Consolidate BlueStep rules into bspecs (atomic files + index)

**Status:** Complete

Each task references specific file paths and is sized for one `/spec-execute` invocation. Order matters — dependent tasks come after the ones they rely on.

Source kits (read-only inputs):
- Brandon: `~/Downloads/BlueStep-Team-Knowledge-Kit/BlueStep-Team-Knowledge-Kit/{01-Platform-Reference,02-Workflow-Conventions,03-Agents}/`
- Brendan: `~/Downloads/Brendan Rules/BSJS_GOTCHAS.md`
- Brian: `~/Downloads/BrianBlueStepMarkdown/{agents.md,agents-support/*}`

## Tasks

### Code infrastructure (do first — foundational, low-risk, independently testable)

- [x] **1. Remove the GitHub Copilot mirror (Claude-only).** Delete `mirrorInstructionsToGithub` and its call site in `scaffold()`, plus any now-unused imports. Files: `src/scaffold.js` (fn at lines ~205–215, call at line 54). Verify `node cli.js -h` still runs and a scratch scaffold produces no `.github/` directory.

- [x] **2. Replace `SYNC_TARGETS` with a full walk of `templates/claude/**` (Claude-only).** _(Done. `enumerateClaudeTargets(exclude)` in `src/utils.js` walks the whole tree; `src/sync.js` hardcoded array deleted, replaced with `enumerateClaudeTargets(SYNC_EXCLUDE)`. Resolves to 19 targets — the prior 18 plus the previously-untracked `skills/task-comment/SKILL.md` (the old list had drifted; `copyTemplateTree` already scaffolds it, so syncing it is consistent). All destRels verified, incl. `settings.json`, `.template.md` spec-templates, and `.md`-stripped instructions.)_ Generalize the helper to walk the **entire** `templates/claude/` tree (not just `instructions/`) and return `{ templateSrc, destRel }` per file, where `destRel` = `.claude/` + the path under `templates/claude/` with a trailing `.template` stripped — the same transform `copyTemplateTree` already applies, which reproduces skills, hooks, `settings.json`, and the `*.template.md` spec-templates (whose `.template.md` is preserved) exactly. Drop the entire hardcoded array. Add a documented `SYNC_EXCLUDE` constant (empty today) as an opt-out for any future scaffold-once file under `claude/`. No `.github` targets. Files: `src/sync.js`, `src/utils.js` (helper next to `copyTemplateTree`). _(Instructions-only dynamic enumeration already landed; this extends it to the whole tree and removes the static entries.)_ Verify: `.claude/bspecs.lock` lists the same files as the old hardcoded list **plus** the new instruction tree; `settings.json` and `spec-templates/*.template.md` resolve to the correct destinations; edit one file locally and confirm `bspecs sync` reports it `skipped (locally modified)`.

### Content migration (the merge)

- [x] **3. Migrate Brandon `01-Platform-Reference/*` → `reference/`.** 30 atomic files + `bluestep-knowledge/design-system.md` → `templates/claude/instructions/reference/<kebab>.md.template`. Per file: kebab-case name (strip `reference_`/`bluestep_`), convert `[[wikilinks]]` → relative markdown links (maintain old-stem→new-path map), strip generic prose (keep BlueStep-specific signal), add `## Contents` TOC if >100 lines, enforce English + consistent terminology (component/field/endpoint). Files: `templates/claude/instructions/reference/*.md.template`. _(Done. 30 files written = 29 from `01-Platform-Reference` + `design-system.md`. **`reference_bluestep_api_patterns.md` deferred to task 6** to avoid colliding with the reconciled `api-patterns.md` — same topic, third source. Frontmatter normalized to `description`-only (Brandon's `metadata`/`originSessionId` memory cruft stripped). Wikilinks: reference→sibling, feedback→`../conventions/`, project→dropped (text kept); the mislabeled `[[reference_bluestep_date_format]]` corrected to `../conventions/date-format.md`. `design-system.md` got a `## Contents` TOC.)_

- [x] **4. Migrate Brandon `02-Workflow-Conventions/*` → `conventions/`.** 13 `feedback_*` files → `templates/claude/instructions/conventions/<kebab>.md.template`. Same treatment as task 3, plus normalize to imperative voice (directives, not observations). Files: `templates/claude/instructions/conventions/*.md.template`. _(Done. 13 files written, 1:1 from the source kit. Names strip `feedback_`/`bluestep_`; `feedback_bluestep_workflow` → `always-snapshot.md` to satisfy the existing link from `reference/merge-report-memo-json.md`. All 9 conventions filenames that task-3 reference files link to (`blueiq-no-ai-branding`, `date-format`, `formula-patterns`, `push-inner-draft`, `separate-files`, `single-script`, `top-level-const-tdz`, `tsc-rootdir`, `always-snapshot`) resolve. Frontmatter normalized to `description`-only (Brandon `name`/`metadata`/`originSessionId`/`type` cruft stripped). Wikilinks: `[[feedback_bluestep_single_script]]` → sibling `single-script.md`; `[[reference_bluestep_api_patterns]]` → `../reference/api-patterns.md` (deferred to task 6 — will resolve then); `[[project_*]]` → link dropped, text kept. Observation-style leads (`endpoint-approach`, `formula-patterns`) rewritten as directives. No file exceeds 100 lines, so no TOCs needed. **Known intra-Brandon tension to reconcile in task 10:** `always-snapshot.md` (push→pull→snapshot, ".build skipped by push.js") vs the newer `snapshot-integrity.md` (2026-05-01: include locally-compiled `.build` in `saveState`) — both migrated faithfully and cross-linked, not merged.)_

- [x] **5. Create `gotchas/`.** Brendan `BSJS_GOTCHAS.md` (split into one file per gotcha if it holds more than one) + Brandon `bluestep-knowledge/common-gotchas.md` → `templates/claude/instructions/gotchas/*.md.template`. Use the collapsible "old patterns" `<details>` block for superseded-version content. Files: `templates/claude/instructions/gotchas/*.md.template`. _(Done. 2 files. Brendan's file holds exactly one gotcha → `fetched-resource-code.md.template` (single atomic file with `description` frontmatter; running-list preamble + "exported from auto-memory" footer cruft dropped). Brandon's `common-gotchas.md` → single `common-gotchas.md.template` per the design mapping table (kept H1 + added `## Contents` TOC, 290 lines — matching the sibling `design-system.md` precedent, no YAML frontmatter since the source had none). SweetAlert2 v8.18.4 written as the body; the v11 "do not use" API moved into a collapsible `<details>` block per the superseded-version rule. Neither source had wikilinks. **Heavy topic overlap with task-6 shared docs (component-library, file-execution, code-patterns) and the conventions tree (Query Access ≈ formula-patterns) left intact — dedup/reconcile is task 10's scope.**)_

- [x] **6. Reconcile the four shared topic guides → `reference/`.** Diff Brian `agents-support/{api-patterns,code-patterns,component-library,file-execution}.md` against Brandon `bluestep-knowledge/` equivalents; produce one canonical file each under `reference/`. **For `api-patterns`, also fold in the third source `01-Platform-Reference/reference_bluestep_api_patterns.md` (293 lines, deferred from task 3).** Merge Brian-unique deltas (`mergeTag()` L/F/I/H codes, `B.net.pageContent()` placements, SVG-icon endpoint requirement, component-library CSS anti-pattern, SweetAlert2 v8). Genuine conflicts → inline `<!-- CONFLICT: … -->` + index roll-up (task 8). Keep each file self-contained. Files: `templates/claude/instructions/reference/{api-patterns,code-patterns,component-library,file-execution}.md.template`. _(Done. 4 canonical files written, each with H1 + `## Contents` TOC (all >100 lines, matching the `design-system.md` precedent — no YAML frontmatter). Brian ≈ Brandon-bluestep-knowledge for all four (common ancestor); merged Brian-unique deltas: `B.net.pageContent()` placement list + detailed request/response handling (code-patterns), component-library CSS anti-pattern + Component Source Priority + color palette + `type="module"` (component-library), Workspace Sync `.b6p_metadata.json` (file-execution). Merged Brandon-unique: Query Patterns + per-row queue/lazy-init (code-patterns), config.json+short-import (component-library), inline Query Access + Common Mistakes (file-execution). api-patterns also folds in the full 01-Platform-Reference third source (writable, B.time dates, MEF entries, summaryUrl, B.net endpoints, base64, B.commit/triggers, single-select, form-save interception). **Reconciliation calls:** genericComponents import → config.json+`'genericComponents'` recommended, full-URL fallback (default+escape-hatch, no flag); `request.content()` preferred over `request.optParameter('body')`; `type="module"` is the correct/superseded form. SweetAlert2 v8 API kept in code-patterns examples (cross-linked to the gotchas <details>). **1 genuine CONFLICT flagged inline** in `api-patterns.md` (client-ID field key `sysId` vs `systemID`) for the task-8 index roll-up; code-patterns uses `sysId` to stay consistent with the 01-ref canonical. The previously-deferred `../reference/api-patterns.md` link (from task 4 `conventions/separate-files`) now resolves. Heavy intra-set + cross-tree overlap (Query Patterns, queue/lazy-init, form-save) left for task 10 to collapse; files kept self-contained per the task.)_

- [x] **7. Fold the two overview-level shared docs into the Tier-2 overviews.** Reconcile Brian/Brandon `platform-overview` → fold unique deltas into `b6p-platform.md.template`; `typescript-guide` → fold into `bsjs-development.md.template`. Do **not** create separate files for these. Add a `## Contents` TOC to each overview (both will exceed 100 lines). Ensure folded content does not duplicate the atomic files (link to them instead). Files: `templates/claude/instructions/b6p-platform.md.template`, `templates/claude/instructions/bsjs-development.md.template`. _(Done. `platform-overview` was identical across Brian/Brandon. Folded its unique deltas into `b6p-platform.md` as a new "Platform architecture" section: the three apps (Relate/Connect/Manage) + BsJs, the Relate config model (Type → Category → Form → Field, marked as the configuration view vs. the existing runtime/storage "Data hierarchy"), and "MergeReports are embedded, not standalone" (`B.siteNavigation()`, `B.isLayout("MANAGE")`, `B.optUser`/`B.user`) linking out to `reference/merge-report-static-index.md` + `reference/component-library.md` instead of duplicating. `typescript-guide` differed (Brandon: "Working with Any Types"/"Type Guards"; Brian: "Graal Compatibility"/"TypeScript Strict Mode"); folded only the BlueStep-specific union into `bsjs-development.md`: a null-safe-reads note (`.opt().orElse()`) under "Reading and writing fields" and a new "TypeScript configuration & Graal compatibility" section (tsconfig + `strict: false`, GraalVM server-side caveats), linking to `conventions/tsc-rootdir.md`. Dropped the guide's generic TS material (classes, singleton, enums, custom errors) per best-practice #5, and its client-side `async`/`await`+`fetch().json()` examples — instead added a note that server-side `B.net.fetch` is synchronous and async patterns apply only to MergeReport `static/` client JS (reconciling the source's misleading async framing). **1 genuine CONFLICT flagged inline** in `b6p-platform.md` Data-hierarchy section (U###### = Unit folder per this overview vs. = Organization ID per Brandon's platform-overview) for the task-8 index roll-up. Both overviews now have a `## Contents` TOC; 191 / 430 lines; no wikilinks; all referenced atomic files resolve.)_

### Indexing & wiring

- [x] **8. Author `index.md.template`.** One entry per file under `reference/`/`conventions/`/`gotchas/` in the form `path — <what it covers>. Load when <trigger>.` with BlueStep keywords; links directly (one hop) to every file. Add a `grep -ri "<term>" .claude/instructions/` discovery hint and an "Unresolved conflicts" roll-up section listing every inline `CONFLICT` flag. Third person, no `@`-imports. Files: `templates/claude/instructions/index.md.template`. _(Done. 49 entries = all 33 `reference/` + 13 `conventions/` + 2 `gotchas/` files, each a one-hop markdown link in the `path — what it covers. Load when <trigger>.` form with BlueStep keywords; verified 1:1 against disk (no orphans, no dangling links). Grouped by directory under H2s, with a short read-on-demand/no-`@`-import preamble (third person). Term-level `grep -ri` discovery hint included. "Unresolved conflicts" roll-up lists both inline flags found by `grep -rn CONFLICT`: `reference/api-patterns.md` (`sysId` vs `systemID` client-ID key, from task 6) and `b6p-platform.md` (`U######` = Unit folder vs Organization ID, from task 7). No wikilinks, no `@`-imports. Added a `description` frontmatter so the index is consistent with the rest of the tree; CLAUDE.md wiring is task 9.)_

- [x] **9. Wire `CLAUDE.md.template` to the tree.** In "Deep reference", add a pointer instructing Claude to consult `.claude/instructions/index.md` first for platform/BsJs detail (read-on-demand wording kept, no `@`-imports). Update "Self-improvement" to mention the index/atomic files. Leave the 8 Tier-1 rules untouched. Files: `templates/root/CLAUDE.md.template`. _(Done. "Deep reference" rewritten: the deep ref still says explicitly not auto-imported / read-on-demand; added a "consult `.claude/instructions/index.md` first" pointer describing the manifest + the `grep -ri` route, kept the two overviews (b6p-platform updated to mention Relate/Connect/Manage architecture from task 7), and noted reaching past the overviews into indexed atomic files. "Self-improvement" step 2 now routes a discovered rule to the right tier: must-always → Critical rules; high-frequency → an overview; single-topic/sharp-edge → a new/existing `{reference,conventions,gotchas}/` file plus an `index.md` entry. No `@`-imports introduced (verified `grep`); all 8 Tier-1 rules untouched. This edits the scaffolded `CLAUDE.md.template`; this repo's own docs are task 12.)_

### Review & docs

- [x] **10. No-duplication + link-integrity review.** Mechanical pass: grep for repeated headings/code blocks/distinctive sentences across `templates/claude/instructions/**`; `grep -r '\[\[' templates/claude/instructions` must return nothing; spot-check converted links resolve. Semantic pass (optionally delegate to a subagent reading the whole tree): catch paraphrased duplicates, including overview-vs-atomic overlap. Collapse each duplicate to one canonical file + link (one level deep). Files: across `templates/claude/instructions/**` (edits as needed). _(Done. **Link integrity:** zero `[[wikilinks]]`; all 49 `index.md` entries map 1:1 to the 49 files on disk (34 reference + 13 conventions + 2 gotchas — no orphans, no dangling); every `../`- and sibling-relative cross-link and the two overview backtick-paths (`reference/merge-report-static-index.md`, `reference/component-library.md`) resolve. **No-duplication:** the one real violation was `gotchas/common-gotchas.md` — Brandon's pre-split roundup that re-taught, with near-identical code blocks, content now owned by dedicated atomic files. Rewrote it into a concise anti-pattern checklist: each duplicated topic (Java optionals, Java collections, server/client split, MergeReport HTML, script-timing race, query access, component library, error handling, strict/Graal) reduced to a sharp-edge warning + a secondary-nav pointer to its canonical file (`api-patterns`/`file-execution`/`code-patterns`/`component-library`/`bsjs-development`); kept full content only where there's no dedicated home (NPM packages; SweetAlert2 — heading text preserved so `code-patterns`'s existing `#sweetalert2-version-differences` link stays valid). File dropped from 290→~95 lines (TOC removed, now <100). Verified single-sourcing by grepping the distinctive comments — each survives in exactly one file. **Over-flags left intact** (already summary+link, not duplication): `merge-report-memo-json:20` and `crm-dashboard-inspo:10` one-liner cross-links; `b6p-platform:34-40` (summary + "Detail:" pointer); the `bsjs-development` module-structure tree (whole-project pull layout — a different view than `file-execution`'s execution-entry-point list). No `.github` mirror exists to re-sync (removed in task 1).)_ _(Follow-up reconciliation: caught a cross-file contradiction on `objects/imports.ts` — the overviews called it a "legacy artifact" while `api-patterns`/`file-execution`/`code-patterns`/`formula-patterns`/`common-gotchas` still taught it as the "(preferred)" query-access pattern. Confirmed with the user that it is deprecated: query/form/field availability is now set by the component's platform **form-import config**, regenerated into `declarations/index.d.ts` on `b6p pull`, and a configured query is a **bare top-level variable** in `app.ts` (no `.query()`). Rewrote all five files to that story — current mechanism in the body, the hand-written `imports.ts` `.require()` mechanics moved into a `<details>` legacy block in `api-patterns` (same superseded-pattern convention as SweetAlert2 v11); ad-hoc read-only `B.queries.byFID['fid'].query()` kept as valid. `dpn-dashboard-framework` (a real component built on the legacy manifest) annotated as predating the move. Anchors `#query-access-patterns` / `#query-access-pattern` preserved so existing inbound links stay valid.)_

- [x] **11. ADR for the structural decisions.** Record (a) dynamic `SYNC_TARGETS` enumeration replacing the hardcoded list and (b) Claude-only (Copilot mirror removed). Files: `docs/decisions/instruction-tree-and-claude-only.md`. _(Done. New ADR `docs/decisions/instruction-tree-and-claude-only.md`, modeled on the existing `b6p-cli-distribution.md` format (Status/Date/Context/Decision/Options/Consequences/References). Records (a) the move from the hardcoded `SYNC_TARGETS` array to `enumerateClaudeTargets(SYNC_EXCLUDE)` walking `templates/claude/**` — the `destRel` transform mirrors `copyTemplateTree`, the `templates/claude/**` boundary excludes `root/`+`module/` by construction, `SYNC_EXCLUDE` is the empty escape hatch, and the walk recovered the previously-dropped `skills/task-comment/SKILL.md`; and (b) Claude-only — `mirrorInstructionsToGithub` + call site deleted, no `.github/` tree emitted, single source of truth is the template tree → `.claude/`. Consequences cover the `bspecs sync` regression risk and the harmless stale `.github/instructions/` lingering in older projects (CHANGELOG callout deferred to task 12). Verified against the live code (`src/utils.js`, `src/sync.js`) and confirmed no live `mirrorInstructions`/`.github/instructions` references remain in `src/`. Status: Accepted, dated 2026-06-19.)_

- [x] **12. Sync this repo's docs.** `CLAUDE.md` — update "Architecture" (drop `mirrorInstructionsToGithub`), "What gets scaffolded" (new `instructions/` tree, no `.github` mirror), "Editing templates" (instruction subfolders); remove the "single source of truth for Claude Code and GitHub Copilot" claim. `CHANGELOG.md` — new version entry (rules consolidation, Copilot mirror removed, stale `.github/instructions/` cleanup note for existing projects). `TODO.md` — add the B4 follow-up (convert Brandon `03-Agents/` roles into skills — deferred). Files: `CLAUDE.md`, `CHANGELOG.md`, `TODO.md`. _(Done. **CLAUDE.md:** Architecture line rewritten — `mirrorInstructionsToGithub` dropped, replaced with the Claude-only / single-source statement + ADR pointer; the "Key behaviors" mirror bullet replaced with a "`SYNC_TARGETS` (dynamic)" bullet describing the `enumerateClaudeTargets(SYNC_EXCLUDE)` walk; "What gets scaffolded" instructions line now lists the overviews + `index.md` + `reference/`/`conventions/`/`gotchas/` (read-on-demand, no `.github` mirror); "Editing templates" instruction bullet updated for the subfolders + the "add an `index.md` entry" rule, mirror claim removed. Grepped CLAUDE.md — only the two intentional "no Copilot mirror" mentions remain. **CHANGELOG.md:** new `## [0.6.0] — 2026-06-19` entry (Added: instructions tree + conflict roll-up; Changed: dynamic `SYNC_TARGETS` + extended overviews; Removed: Copilot mirror; plus a "Note for existing projects" callout that `bspecs sync` won't delete a stale `.github/instructions/`). **TODO.md:** new "Rules consolidation follow-ups" section with the B4 item (Brandon `03-Agents/` roles → skills, deferred, citing the spec's Out-of-scope). This repo has no own `README.md`, and these are repo docs (not `templates/claude/instructions/`), so no mirror to reconcile. `package.json` version bump (0.5.0 → 0.6.0) + tag left for the release/commit step, out of this task's scope.)_

### Verification

- [x] **13. End-to-end scaffold verification.** Scaffold a throwaway project; confirm `.claude/instructions/{index.md, reference/, conventions/, gotchas/}` all land, **no** `.github/` tree exists, `bspecs.lock` covers every instruction file, and `bspecs sync` skip-on-local-edit works. `grep -ri "copilot\|mirrorInstructions\|github/instructions" .` (outside `.claude/specs/`) returns nothing live. Spot-check migrated files for resolved links and English. _(Done. Non-interactive scaffold (`scaffold()` into a scratch dir) produced the full tree: `index.md` + `reference/` (34) + `conventions/` (13) + `gotchas/` (2) = 49 atomic files + 2 overviews + index = 52 instruction files, all with `.template` stripped and `{{VARS}}` substituted; **no `.github/` directory**. `bspecs.lock` (69 entries) covers all 52 instruction files with no orphans. `bspecs sync` baseline = 0 changes; after a local edit to `reference/api-patterns.md` it reported `skipped (locally modified)`. **130 markdown `.md` links across the scaffolded tree all resolve; zero `[[wikilinks]]`; no leftover placeholders; no Spanish.** `node cli.js -v/-h` and the refreshed `test-scaffold.mjs` (49 passed, 0 failed) both pass. **Grep gate — fixed live stale references the verification surfaced** (task 12 had wrongly assumed no repo `README.md`): repo `README.md` + `package.json` description dropped the "Claude Code and GitHub Copilot" dual-support claim; **`templates/root/README.md.template` dropped the `.github/instructions/` mirror line — a shipped bug** (every scaffolded README claimed a mirror that's no longer generated); repo dev tooling that named the deleted `mirrorInstructionsToGithub` fn / removed mirror as a live step rewritten (`.claude/skills/{quick-task,spec-create,spec-execute}/SKILL.md`, `.claude/spec-templates/tasks.template.md`); `test-scaffold.mjs` updated to assert **no** `.github/` mirror + the new instruction subfolders, and its stale "no RelateScript anywhere" invariant retired (the migrated reference tree intentionally documents RS↔BSJS equivalences). Remaining grep hits are all legitimate: documentation **of** the removal (CHANGELOG, the ADR, CLAUDE.md negatives), historical records (CHANGELOG 0.1.0; `rule-audit.md` describing the audited *source* template), and the new "no mirror" assertion comment. **One borderline item left for the user:** `docs/bspecs-builder/requirements.md` (the cross-track parent planning doc) still describes the original "generalize `mirrorInstructionsToGithub`" plan that the Claude-only decision superseded — left untouched because Track A is explicitly out of this spec's scope.)_

## Verification

No test suite — confirm manually:

- `node cli.js -v` / `node cli.js -h` still work after the code changes (tasks 1–2).
- Scaffold into a scratch directory; inspect the generated `.claude/instructions/` tree and confirm the absence of `.github/`.
- Re-read a sample of migrated reference/conventions/gotchas files for resolved wikilinks, English, consistent terminology, and TOCs on long files.
- `bspecs sync` against the scratch project: new files added, locally-edited files skipped.

## Wrap-up

- Keep `CLAUDE.md` / `README.md` in sync with the new tree and Claude-only stance (task 12).
- Note the change in `CHANGELOG.md`, including the stale-`.github` cleanup callout (task 12).
- `mirrorInstructionsToGithub` is **removed**, so there is no longer a `.github/instructions/` mirror to keep in sync — verify nothing still references it (task 13).
- Record the B4 follow-up (agent roles → skills) in `TODO.md` (task 12).
